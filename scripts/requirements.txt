# Tooling dependencies. Install into a venv:
#
#   python3 -m venv .venv && .venv/bin/pip install -r scripts/requirements.txt
#
# Pinned exactly so a validation failure is never a dependency drift.
jsonschema==4.23.0
