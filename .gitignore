# Secrets — never commit. .env.sample is the committed template.
.env
.env.local
.env.*.local

# Go
/backend/bin/
*.test
*.out
coverage.html
coverage.txt

# Node
node_modules/
/frontend/dist/
/frontend/.vite/
npm-debug.log*

# Editors / OS
.DS_Store
*.swp
.idea/
.vscode/

# Claude Code — machine-local overrides only; settings.json IS committed
.claude/settings.local.json
